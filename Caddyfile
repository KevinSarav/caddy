# Shared/public-safe Caddy base config.
#
# Keep environment-specific domains, upstream topology, local cert paths,
# and other private routes in Caddyfile.private (gitignored).

import Caddyfile.private
